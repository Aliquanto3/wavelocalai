# Rôle
Tu es un Expert DevOps GitHub Actions.

# Objectif
Créer le pipeline CI/CD pour le projet "WaveLocalAI" (Python/Streamlit).

# Tâche
Génère le fichier `.github/workflows/ci.yml`.

# Spécifications
1.  **Déclencheur :** Push et PR sur `main`.
2.  **OS :** `ubuntu-latest`.
3.  **Matrice :** Python 3.10 et 3.11.
4.  **Steps :**
    - Checkout.
    - Setup Python.
    - Cache PIP (Critique pour la vitesse).
    - Install dependencies (`requirements.txt` + `pytest` + `ruff`).
    - Linting avec `ruff` (mode non-bloquant pour commencer).
    - Testing avec `pytest` (export PYTHONPATH=. pour que les imports src/ fonctionnent).

[COLLER REVIEW_ME.TXT ICI]